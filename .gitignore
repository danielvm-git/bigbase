# Go
bigbase
bigbase-linux-amd64
bigbase-darwin-*
bigbase_linux
*.exe
*.test
*.out
vendor/

# Terraform state — never commit
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Actual vars file — contains secrets
terraform.tfvars

# Crash logs
crash.log
crash.*.log

# OCI keys — just in case
*.pem
bigbase.db
data/
.bigpowers/
*.zip

# Node / semantic-release
node_modules/

# Environment (secrets)
.env

# macOS
.DS_Store

# Playwright E2E
test-results/
playwright-report/

<<<<<<< HEAD
# Frontend build artifacts (rebuildable)
/ui/dist/
/packages/*/dist/

# Allure — generated artifacts, never committed
allure-results/
allure-report/
allure-history/


# Ctxo local cache (auto-rebuilt, never committed)
.ctxo/*
!.ctxo/config.yaml

# Dev tooling (local-only)
.bts.toml
.envrc
.rtk/
.vscode/mcp.json
.greptile/
.pi/
.mimocode/
mimocode.jsonc
<stdin>
.agents/mcp_config.json
agentic-cps/

# Cursor IDE local config (may contain API keys)
.cursor/

# Python bytecode
__pycache__/

# Generated docs (regenerated by gate-trace / skills)
specs/TRACEABILITY_LATEST.md

# Test-generated git repos (must be created in t.TempDir() only)
repo-*.git

# Ctxo local cache (never committed)
.ctxo/.cache/
.zcode/

# IDE / local workspace artifacts
.claude/
specs/.obsidian/
