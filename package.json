{
  "scripts": {
    "preflight": "npm run preflight:go && npm run preflight:build && npm run preflight:secrets",
    "preflight:go": "go vet ./... && go test $(go list ./... | grep -v /deploy) && go run github.com/securego/gosec/v2/cmd/gosec@latest -quiet -exclude-dir=ui/node_modules -exclude=G104,G115,G404,G702,G703,G704,G112,G114,G118,G120,G124,G204,G301,G304,G306,G705,G710 ./...",
    "preflight:build": "go build -o /dev/null .",
    "preflight:secrets": "gitleaks detect --source . --no-banner 2>&1 && echo 'secrets: OK' || echo 'gitleaks: SKIP (not installed)'",
    "preflight:ui": "cd ui && npm run build"
  },
  "name": "bigbase-auth-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^25.0.5"
  }
}
