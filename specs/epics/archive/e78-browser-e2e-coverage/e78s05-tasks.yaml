story_id: e78s05
title: Functions, Messaging & Users
status: todo
bcps: 6
tasks:
  - id: 1
    description: "E2E test for Functions CRUD & Execution Logs (e10, e17). Cover: view function card grid with all deployed functions; click a function to open detail view; navigate through code, triggers, variables, and logs tabs; click Run function and verify execution log appears in logs tab with status code and duration."
    verify: "npx playwright test tests/e2e/functions-ui.spec.ts -g 'Functions CRUD'"
    status: todo
  - id: 2
    description: "E2E test for Messaging Template Editor & Send (e12, e17). Cover: navigate to Messaging page; create new message with subject/body; use template editor to compose content; preview message rendering; send message; verify message appears in outbound log with sent status."
    verify: "npx playwright test tests/e2e/messaging-ui.spec.ts -g 'Messaging Editor'"
    status: todo
  - id: 3
    description: "E2E test for Messaging Template Editor — XSS Sanitization (security). Cover: attempt stored XSS payload in message template content (e.g., <img onerror=alert(1) src=x>); verify preview renders payload as escaped text; verify sent message renders safely in outbound log without script execution."
    verify: "npx playwright test tests/e2e/messaging-ui.spec.ts -g 'Messaging XSS Guard'"
    status: todo
  - id: 4
    description: "E2E test for Users Management — Delete Confirmation (e17). Cover: navigate to Users page; click delete on a user row; verify confirmation modal appears with user details; click cancel — modal dismisses and user not deleted; click confirm delete — verify user removed from listing; verify undo/snackbar notification appears."
    verify: "npx playwright test tests/e2e/users-ui.spec.ts -g 'Users Management'"
    status: todo
  - id: 5
    description: "E2E test for Functions — empty state and error handling. Cover: navigate to Functions page with no deployed functions; verify empty state card with onboarding CTA; trigger function run without required params; verify error state display in logs panel."
    verify: "npx playwright test tests/e2e/functions-ui.spec.ts -g 'Functions Empty State'"
    status: todo
  - id: 6
    description: "E2E test for Messaging — template variables and preview. Cover: create message template with {{variable}} placeholders; fill variable values in UI; verify preview renders with substituted values; verify that sending with missing variables shows validation error."
    verify: "npx playwright test tests/e2e/messaging-ui.spec.ts -g 'Messaging Variables'"
    status: todo
