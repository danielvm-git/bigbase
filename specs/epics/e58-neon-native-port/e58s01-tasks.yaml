story_id: e58s01
title: "SQL-over-HTTP Transport"
status: planned
bcps: 3
security: low
tasks:
  - id: 1
    description: "Create components/api/neon.go with NeonResult, NeonField structs, convertDollarParams helper, and goTypeToDataTypeID helper"
    verify: "go build ./components/api/"
    status: todo

  - id: 2
    description: "Add driver string field to api.Options and api.API; populate in New(); use in handleNeonSQL to choose $N→? conversion"
    verify: "go build ./components/api/"
    status: todo

  - id: 3
    description: "Implement handleNeonSQL on *API: POST-only, decode {query, params}, apply same security rules as handleSQL (admin role, deny-list, single-stmt, read-only), convert params, execute, scan into NeonResult"
    verify: "go build ./components/api/"
    status: todo

  - id: 4
    description: "Register /api/sql/neon route in Handler() and in main.go using the existing sqlHandler chain"
    verify: "go build ."
    status: todo

  - id: 5
    description: "Write unit tests for convertDollarParams (no-op on ?, rewrites $1/$2, handles $10) and goTypeToDataTypeID (int64→23, float64→701, string→25, bool→16, unknown→0)"
    verify: "go test ./components/api/ -run TestConvertDollarParams -v -count=1 && go test ./components/api/ -run TestGoTypeToDataTypeID -v -count=1"
    status: todo

  - id: 6
    description: "Write integration tests for POST /api/sql/neon: admin JWT + valid SELECT → 200 NeonResult shape; no params → treated as empty; non-admin → 403; internal table → 403; write query → 400"
    verify: "go test ./components/api/ -run TestNeon -v -count=1"
    status: todo

  - id: 7
    description: "Run full test suite to confirm no regressions from new route and driver field changes"
    verify: "go test ./... -count=1"
    status: todo
