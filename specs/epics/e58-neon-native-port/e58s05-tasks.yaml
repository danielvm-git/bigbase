story_id: e58s05
title: "Astro SDK Refactor"
status: planned
bcps: 2
security: none
tasks:
  - id: 1
    description: "Add setToken(token: string): void to AuthClient interface and createAuthClient implementation in packages/auth/src/index.ts (sets internal storedToken)"
    verify: "cd packages/auth && npm run typecheck"
    status: todo

  - id: 2
    description: "Build @bigbase/auth to emit updated types"
    verify: "cd packages/auth && npm run build"
    status: todo

  - id: 3
    description: "Fix createBigBaseAuth in packages/auth-astro/src/index.ts: call client.setToken(token) before client.getSession() when token is extracted from cookie"
    verify: "cd packages/auth-astro && npm run build"
    status: todo

  - id: 4
    description: "Fix AstroAuth.session type from 'any' to 'AuthSession | null'; fix requireAuth return type to 'AstroAuth | Response'; export AstroAuthLocals type"
    verify: "cd packages/auth-astro && npm run build"
    status: todo

  - id: 5
    description: "Write unit test for setToken: call setToken('abc'), then verify getJWT() returns 'abc' without any fetch calls"
    verify: "cd packages/auth && npm test"
    status: todo

  - id: 6
    description: "Write unit test for createBigBaseAuth middleware: mock fetch; with cookie 'token=jwt123' verify fetch called with Authorization: Bearer jwt123; without cookie verify locals.auth is null"
    verify: "cd packages/auth && npm test"
    status: todo

  - id: 7
    description: "Final build and test for both packages"
    verify: "cd packages/auth && npm run build && npm test && cd ../auth-astro && npm run build"
    status: todo
