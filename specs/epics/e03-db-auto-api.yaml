type: epic-shard
context: domain
id: e03
title: "DB + Auto API"
wsjf: 1.0
wsjf_rationale: "Historical — foundation epic. BV=10(data persistence) TC=10(all features need DB) RR=10 / JS=5"
status: done
migrated_from: specs/archive/slices/003-db-auto-api.md
stories:
  - id: e03s01
    title: "SQLite-backed auto CRUD collections API"
    status: done
    tasks:
      - desc: "POST /api/collections/:name creates records"
        verify: "curl -X POST http://localhost:9999/api/collections/test -d '{}'"
      - desc: "GET /api/collections/:name lists records"
        verify: "curl http://localhost:9999/api/collections/"
      - desc: "/api/sql endpoint for raw queries"
        verify: "curl -X POST http://localhost:9999/api/sql -d '{\"query\":\"SELECT 1\"}'"
