type: epic-shard
context: domain
id: e24
title: Wire Observability
wsjf: 5.0
status: pending
covers: []
stories:
  - id: e24s01
    title: Standardize structured logging
    status: pending
    tasks:
      - desc: All components use slog via injected Logger; --log-level flag
        verify: "go run . serve --log-level debug 2>&1 | head -5 | grep -q '{'"
  - id: e24s02
    title: Distributed request ID
    status: pending
    tasks:
      - desc: X-Request-ID middleware in proxy with context propagation
        verify: "curl -sI http://localhost:9999/ | grep -qi x-request-id"
  - id: e24s03
    title: Per-component health
    status: pending
    tasks:
      - desc: Extend /health with per-component status map
        verify: "curl -sf http://localhost:9999/health | grep -q components"
  - id: e24s04
    title: Prometheus metrics export
    status: pending
    tasks:
      - desc: GET /api/monitoring/metrics/prometheus text format
        verify: "curl -sf http://localhost:9999/api/monitoring/metrics/prometheus | grep -q bigbase_"
  - id: e24s05
    title: Idempotent setup scripts
    status: pending
    tasks:
      - desc: scripts/setup.sh idempotent and scripts/health-check.sh
        verify: "bash scripts/setup.sh && bash scripts/health-check.sh"
