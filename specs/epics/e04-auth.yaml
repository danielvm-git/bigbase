type: epic-shard
context: domain
id: e04
title: "Auth — Register, Login, JWT"
wsjf: 1.0
wsjf_rationale: "Historical — foundation epic. BV=10(user identity) TC=10(all protected routes need auth) RR=10 / JS=5"
status: done
migrated_from: specs/archive/slices/004-auth.md
stories:
  - id: e04s01
    title: "Email/password register and login with JWT"
    status: done
    tasks:
      - desc: "POST /api/auth/register creates user and returns JWT"
        verify: "curl -X POST http://localhost:9999/api/auth/register -d '{\"email\":\"a@b.com\",\"password\":\"x\"}'"
      - desc: "POST /api/auth/login returns JWT"
        verify: "curl -X POST http://localhost:9999/api/auth/login -d '{\"email\":\"a@b.com\",\"password\":\"x\"}'"
