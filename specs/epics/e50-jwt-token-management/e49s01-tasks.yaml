story_id: e49s01
title: "Persist JWT secret — env var with fallback to auto-generate"
status: planned
bcps: 2
tasks:
  - id: 1
    description: "Add --jwt-secret CLI flag and BIGBASE_JWT_SECRET env var"
    verify: "go build ./... && go run . serve --help 2>&1 | grep -c 'jwt-secret'"
    status: todo
  - id: 2
    description: "Pass Secret to auth.New() from config; auto-generate if empty (with warning)"
    verify: "go test ./components/auth/ -run TestJWTSecretFromConfig -v"
    status: todo
  - id: 3
    description: "Validate provided secret: min 32 bytes, not a well-known default"
    verify: "go test ./components/auth/ -run TestJWTSecretValidation -v"
    status: todo
  - id: 4
    description: "Add --dev flag to suppress auto-generated secret warning in development"
    verify: "go test ./components/auth/ -run TestDevModeSuppressWarning -v"
    status: todo
