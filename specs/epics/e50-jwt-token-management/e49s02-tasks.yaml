story_id: e49s02
title: "Configurable token lifetimes"
status: planned
bcps: 2
tasks:
  - id: 1
    description: "Add --jwt-access-expiry and --jwt-refresh-expiry CLI flags with env var fallbacks"
    verify: "go build ./... && go run . serve --help 2>&1 | grep -c 'jwt-access-expiry'"
    status: todo
  - id: 2
    description: "Parse durations, validate ranges (5m-168h access, 1h-2160h refresh)"
    verify: "go test ./components/auth/ -run TestTokenLifetimeValidation -v"
    status: todo
  - id: 3
    description: "Replace hardcoded durations in jwt.go and refreshtoken.go with config fields"
    verify: "go test ./components/auth/ -run TestTokenLifetimeConfig -v"
    status: todo
  - id: 4
    description: "Add expires_at and expires_in fields to auth response JSON"
    verify: "go test ./components/auth/ -run TestAuthResponseExpiry -v"
    status: todo
