type: epic-shard
context: infra
id: e02
title: "Proxy + Landing Page"
wsjf: 1.0
wsjf_rationale: "Historical — foundation epic. BV=10(HTTP entry point) TC=10(required for all HTTP features) RR=10 / JS=3"
status: done
migrated_from: specs/archive/slices/002-proxy-landing.md
stories:
  - id: e02s01
    title: "HTTP proxy with routing and landing page"
    status: done
    tasks:
      - desc: "GET /api/ping returns status and version"
        verify: "curl http://localhost:9999/api/ping"
      - desc: "GET / renders landing page"
        verify: "curl -s http://localhost:9999/ | head -5"
